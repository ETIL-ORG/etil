# ETIL MCP Server — Production Environment Variables
# Copy to .env and set a strong random API key:
#   cp .env.example .env
#   openssl rand -hex 32 >> .env  # then edit to set as ETIL_MCP_API_KEY value
#   chmod 600 .env

ETIL_MCP_API_KEY=CHANGE_ME_TO_A_STRONG_RANDOM_KEY
